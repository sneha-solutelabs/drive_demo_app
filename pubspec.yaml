name: drive_poc
description: A new Flutter project.

publish_to: 'none' # Remove this line if you wish to publish to pub.dev

version: 1.0.0+1

environment:
  sdk: ">=2.16.0-80.1.beta <3.0.0"

dependencies:
  flutter:
    sdk: flutter


  cupertino_icons: ^1.0.2

  flutter_deriv_bloc_manager:
    git:
      url: https://github.com/regentmarkets/flutter-deriv-packages.git
      path: packages/deriv_bloc_manager
      ref: dev

  deriv_theme:
    git:
      url: git@github.com:regentmarkets/flutter-deriv-packages.git
      path: packages/deriv_theme
      ref: dev

  flutter_deriv_api:
    git:
      url: git@github.com:regentmarkets/flutter-deriv-api.git
      ref: dev

  flutter_localizations:
    sdk: flutter

  flutter_deriv_theme:
    path: ./flutter_deriv_theme/


  equatable: ^2.0.2


dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^1.0.0
  mockito: ^5.0.8
  mocktail: ^0.2.0
  bloc_test: ^8.0.0
  build_runner:

  deriv_lint:
    git:
      url: git@github.com:regentmarkets/flutter-deriv-packages.git
      path: packages/deriv_lint
      ref: dev

flutter:
  uses-material-design: true
